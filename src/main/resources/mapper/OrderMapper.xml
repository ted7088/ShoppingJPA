<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.shoppingjpa.mapper.OrderMapper">

    <!-- Order ResultMap -->
    <resultMap id="OrderResultMap" type="com.example.shoppingjpa.vo.Order">
        <id property="id" column="id"/>
        <result property="userId" column="user_id"/>
        <result property="orderNumber" column="order_number"/>
        <result property="totalAmount" column="total_amount"/>
        <result property="status" column="status"/>
        <result property="paymentMethod" column="payment_method"/>
        <result property="createdAt" column="created_at"/>
    </resultMap>

    <!-- OrderItem ResultMap -->
    <resultMap id="OrderItemResultMap" type="com.example.shoppingjpa.vo.OrderItem">
        <id property="id" column="id"/>
        <result property="orderId" column="order_id"/>
        <result property="productId" column="product_id"/>
        <result property="productName" column="product_name"/>
        <result property="productPrice" column="product_price"/>
        <result property="quantity" column="quantity"/>
        <result property="subtotal" column="subtotal"/>
    </resultMap>

    <!-- 주문 생성 -->
    <insert id="insertOrder" parameterType="com.example.shoppingjpa.vo.Order" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO orders (user_id, order_number, total_amount, status, payment_method, created_at)
        VALUES (#{userId}, #{orderNumber}, #{totalAmount}, #{status}, #{paymentMethod}, #{createdAt})
    </insert>

    <!-- 주문 상품 추가 -->
    <insert id="insertOrderItem" parameterType="com.example.shoppingjpa.vo.OrderItem" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO order_items (order_id, product_id, product_name, product_price, quantity, subtotal)
        VALUES (#{orderId}, #{productId}, #{productName}, #{productPrice}, #{quantity}, #{subtotal})
    </insert>

    <!-- 주문 조회 (ID) -->
    <select id="findById" resultMap="OrderResultMap">
        SELECT * FROM orders WHERE id = #{id}
    </select>

    <!-- 주문 번호로 조회 -->
    <select id="findByOrderNumber" resultMap="OrderResultMap">
        SELECT * FROM orders WHERE order_number = #{orderNumber}
    </select>

    <!-- 사용자별 주문 목록 -->
    <select id="findByUserId" resultMap="OrderResultMap">
        SELECT * FROM orders WHERE user_id = #{userId} ORDER BY created_at DESC
    </select>

    <!-- 주문 상품 목록 조회 -->
    <select id="findOrderItemsByOrderId" resultMap="OrderItemResultMap">
        SELECT * FROM order_items WHERE order_id = #{orderId}
    </select>

    <!-- 주문 상태 업데이트 -->
    <update id="updateOrderStatus">
        UPDATE orders SET status = #{status} WHERE id = #{id}
    </update>

</mapper>
